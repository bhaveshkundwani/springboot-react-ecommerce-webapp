spring:
  application:
    name: spring-boot-ecom-project

  datasource:
    url: jdbc:postgresql://localhost:5432/ecommdb
    username: postgres
    password: bhavesh18
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true